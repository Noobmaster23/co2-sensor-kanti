#ifndef CLé // To make sure you don't declare the function more than once by including the header multiple times.
#define CLé

/**
 * Uploads the data given in the parameters to the PostgreSQL database.
 * */
void sendData();

#endif
