#include "clé.h"
#include "SimplePgSQL.h"

void sendData(int temperature, char datetime[], int roomnumber)
{
    PGconnection(Client * c, int flags = 0, int memory = 0, char *foreignBuffer = NULL);
}